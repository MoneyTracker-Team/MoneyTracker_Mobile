import React from 'react';
import { View, Text, Button } from 'react-native';
import styles from './statisticColumn.styles.js';

const StatisticColumnScreen = ({ navigation }) => {
  return (
    <View>
      <Text style={styles.example}>This is StatisticColumn Screen</Text>
    </View>
  );
};

export default StatisticColumnScreen;
